// /edit and /fix are the same editor — no draft/publish flow on this menu,
// every change saves straight to the live menu. See src/app/dessert/edit/page.tsx.
export { default } from '../edit/page';
